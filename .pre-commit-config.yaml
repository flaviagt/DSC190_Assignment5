repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    # Use the latest stable version
    rev: v0.15.12
    hooks:
      # Run the linter (checks only - no auto-fixes)
      - id: ruff-check
      # Run the formatter
      - id: ruff-format